version: '3.7'

services:
  postgres:
    image: postgres:14.3
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: prisma
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
    volumes:
      - ./src/prisma/tmp/postgres:/var/lib/postgresql/data
